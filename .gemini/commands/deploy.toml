description = "Prepare the current project for Vercel deployment"

prompt = """
Prepare the current project for Vercel deployment.

Do not deploy production without explicit approval.

Inspect:

1. package.json
2. build scripts
3. framework
4. Vercel configuration
5. environment variables
6. API routes
7. serverless/edge functions
8. database configuration
9. build output
10. security configuration

Run the appropriate build and tests.

If Vercel MCP is available, inspect the deployment configuration.

Create or verify a preview deployment when appropriate.

Do not expose secrets.

Do not modify unrelated functionality.
"""
