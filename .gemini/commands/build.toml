description = "Build and validate the current application"

prompt = """
Build and validate the current application.

First inspect the repository and existing scripts.

Then:

1. Install missing dependencies only when necessary.
2. Run the project build.
3. Run type checking if available.
4. Run linting if available.
5. Run automated tests.
6. Start the application if needed.
7. Use Playwright to test important user flows.
8. Check browser console errors.
9. Check network errors.
10. Review git diff.

Fix issues you discover when they are clearly related to the current task.

Do not redesign existing UI unless explicitly requested.

At the end report:

BUILD
TYPECHECK
LINT
TESTS
PLAYWRIGHT
CONSOLE
GIT DIFF

with PASS/FAIL status.
"""
